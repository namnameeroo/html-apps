# 백로그

앞으로 할 일을 모아둔다. 각 항목은 한 줄 요약 + 출처/맥락을 함께 적어, 에이전트가 맥락 재질문 없이 집어갈 수 있게 한다.

- 작업을 끝내면 항목을 제거하거나 `~~취소선~~` 처리하고, 관련 결정이 생겼으면 `decisions/`에 ADR을 남긴다.
- 큰 항목은 `[영역]` 태그로 묶는다.
- 그루밍은 `doc-keeper` 에이전트가 담당한다.
